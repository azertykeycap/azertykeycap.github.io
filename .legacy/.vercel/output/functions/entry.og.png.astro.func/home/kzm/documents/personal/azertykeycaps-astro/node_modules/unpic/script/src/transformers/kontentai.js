"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transform = exports.parse = void 0;
const utils_js_1 = require("../utils.js");
const parse = (url) => {
    const parsedUrl = (0, utils_js_1.toUrl)(url);
    const fit = parsedUrl.searchParams.get("fit") || undefined;
    const width = (0, utils_js_1.getNumericParam)(parsedUrl, "w");
    const height = (0, utils_js_1.getNumericParam)(parsedUrl, "h");
    const quality = (0, utils_js_1.getNumericParam)(parsedUrl, "q");
    const format = parsedUrl.searchParams.get("fm") || undefined;
    parsedUrl.search = "";
    return {
        width,
        height,
        format,
        base: parsedUrl.toString(),
        params: { fit, quality },
        cdn: "kontent.ai",
    };
};
exports.parse = parse;
const transform = ({ url: originalUrl, width, height, format }) => {
    const url = (0, utils_js_1.toUrl)(originalUrl);
    (0, utils_js_1.setParamIfDefined)(url, "w", width, true, true);
    (0, utils_js_1.setParamIfDefined)(url, "h", height, true, true);
    (0, utils_js_1.setParamIfDefined)(url, "fm", format, true);
    if (width && height) {
        (0, utils_js_1.setParamIfUndefined)(url, "fit", "crop");
    }
    return url;
};
exports.transform = transform;
